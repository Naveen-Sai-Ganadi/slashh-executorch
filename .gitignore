# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# Autonomy OS generated state
.claude/state/green.json
.claude/state/run-log.jsonl
.claude/settings.local.json

# Credentials — never commit (live AI Hub token lives in ~/.qai_hub/client.ini)
client.ini
*.token
.env

# Local datasets (real audio is downloaded, not vendored)
data/

# JVM unit-test cache (downloaded jars + compiled classes)
android/.jvmtest/

# Android build / IDE / machine-specific (never commit)
android/.gradle/
android/.idea/
android/build/
android/app/build/
android/local.properties

# QNN/QAIRT SDK (large, downloaded — not vendored)
.qairt/
.qairt.zip

# QNN runtime libs — proprietary, regenerate via tools/qnn/build_qnn_pte.sh
android/app/src/main/jniLibs/arm64-v8a/*.so
