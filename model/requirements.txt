# Slashh AI — model (AOT) side. Verified install, Python 3.11.
# Pins match the toolchain that produced the working .pte + parity (plan §8.1).
torch==2.11.0
torchaudio==2.11.0
executorch==1.2.0
qai-hub==0.50.0
qai-hub-models==0.56.0
torchao==0.17.0
numpy==2.4.4
pytest==8.4.2
